{
  "name": "reconnecting-alive-socket",
  "version": "1.0.2",
  "description": "A typescript library to realize reconnecting websocket",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "version": "npm run build && git add .",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "reconnecting",
    "websocket",
    "alive"
  ],
  "author": "MingDongShenSen",
  "license": "ISC",
  "files": [
    "lib/**/*"
  ],
  "exports": {
    ".": "./lib/index.js"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
